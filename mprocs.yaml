procs:
  next:
    cwd: .
    cmd: ["npm", "run", "dev"]

  inngest:
    cwd: .
    cmd: ["npm", "run", "inngest:dev"]

  redis:
    cwd: .
    cmd: ["docker", "run", "--rm", "-p", "6379:6379", "redis:7-alpine"]
